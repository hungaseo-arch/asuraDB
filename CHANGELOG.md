# CHANGELOG

AsuraDB 의 **릴리스 단위 요약**. 큰 흐름만 담는다.

> 개별 변경의 상세 기록(무엇을·어디서·검증 결과)은
> [docs/웹사이트_운영_변경이력.md](docs/웹사이트_운영_변경이력.md) 가 SSOT 다.
> 이 파일은 그 기록을 월 단위로 압축한 것이므로, **매 작업마다 여기에 추가하지 않는다.**
> 기능 묶음이 마무리되거나 배포 단위가 끊길 때만 갱신한다.

버전 태그는 아직 쓰지 않는다. 날짜는 절대표기(자카르타 기준).

---

## 2026-09 — 근태 관리 · 계정 보안

### 근태 관리 (신규 도메인)

- **대시보드 3화면** — 출퇴근 현황(`/attendance`) · 휴가/초과근무 승인(`/leave-management`) ·
  근태 집계(`/attendance-report`).
- **직원용 PWA** — `employee-pwa/`, `/asuraDB/pwa/` 에 별도 배포. 출퇴근·이력·휴가신청.
- **지오펜싱** — 사업장 반경 판정 + Leaflet/OSM 미니 지도. 근무 중 5분 간격 좌표 보고.
- **대리출석 방지** (Phase 1 → 3)
  - Phase 1: 셀피 촬영 의무화, 비공개 버킷(`attendance-selfies`), 90일 경과분 자동 삭제
    (Edge Function `attendance-selfie-cleanup` + pg_cron).
  - Phase 2: 출퇴근·정정·취소·수기추가·기기등록을 **SECURITY DEFINER RPC 로만** 가능하게 하고
    `attendance` 직접 INSERT 경로를 전부 차단. 관리자 정책도 SELECT 전용으로 축소.
  - Phase 2.1: 셀피 검증(본인 폴더·15분 이내·미사용 객체), 버킷 2 MB/`image/jpeg` 제한,
    `pg_advisory_xact_lock` 으로 중복 출퇴근 차단, 역할 비교 fail-open(`<>` → `IS DISTINCT FROM`) 수정,
    트리거 전용 함수의 PUBLIC EXECUTE 회수.
  - Phase 3: 대시보드·PWA 를 새 RPC 시그니처에 맞춰 전환.
- 상세: [docs/근태관리기능.md](docs/근태관리기능.md)

### 계정 · 보안

- 로그인/로그아웃 이력 기록 + 관리자 조회 화면(`/login-history`).
- 계정 메뉴 통합, 비밀번호 변경 자가서비스, **30분 미조작 자동 로그아웃**.
- CSV 인젝션 방지, 로그 위조 방지 RLS 강화, 지도 팝업 XSS 수정.

### 그 밖에

- DOT 공장코드 조회 결과 패널 정렬 + 국가별 공장 목록 기본 표시.
- 지점 실적 매출/P&L 표 디자인을 `LaborCost` 기준으로 정리.
- 폴더 덮어쓰기로 소실됐던 Edge Function·마이그레이션 복원.

---

## 2026-08 — 디자인 통일 · 보안 점검

- **60-30-10 디자인 가이드 전면 적용** — 전역 토큰 재정의 → 공통 컴포넌트 → 차트 시리즈 색 →
  고밀도 화면 4종 → 나머지 11개 뷰·`lib` 까지 순차 토큰화.
- **입력창 규격 확정**(2026-08-19) — 표 인라인 편집은 평문형, 라벨 폼은 `.form-grid`/`.form-field`.
  테두리 박스형 인풋 폐지.
- **마진·VAT 기준 확정**(2026-08-19) — 마진은 항상 판매가 기준, 대리점가만 VAT 포함가.
- 외부 보안 점검 P1·P2 조치, RLS 사각지대 봉쇄, HTTPS 배포본의 localhost 호출 제거.
- DOT 조회 화면, SAP/스펙 데이터 정비, 학습덱 추가.

---

## 2026-07 — 대시보드 본격 구축

- 대시보드 · 수집기 · DB 마이그레이션 기반 구축.
- 인건비 탭(`/labor-cost`), 개인 자료실(`/seo-docs`) 추가. 마진·지표 정비.
- **라이트 모드 앱 표준 확정**(2026-07-29). 전 뷰 디자인을 인건비 탭 기준으로 통일.
- 타이어 수입통계 표 정렬·합계 표기 수정. 고아 테이블 `branch_ops_monthly` 정리.

---

## 2026-05 — 초기

- 저장소 개설. Vue 3 + Vite + Supabase 골격.
- 하드코딩돼 있던 Supabase 키를 소스에서 제거하고 환경변수로 이관.
